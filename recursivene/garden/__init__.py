"""recursivene.garden — the gardener loop.

The closure proves the MECHANISM of self-improvement once. The garden is what you do with
it: tend the seed across many seasons and watch it grow its own scope. Each season the
learner masters its world, the generator reaches for a harder problem at the frontier, and
when the frontier EXHAUSTS at the current capacity the system GROWS — it widens its
perceptual bandwidth (gamma) and capacity (D) just enough to make the next rung learnable —
then logs a growth ring. Run it long enough and the rings tell you whether the loop
COMPOUNDS (repertoire + complexity keep climbing) or stalls, and at what honest cost.

This is the difference between 'a seed that can grow' and 'a seed that grew'.
"""

from .garden import grow_garden, season, propose_frontier, master_cost, grow_capacity

__all__ = ["grow_garden", "season", "propose_frontier", "master_cost", "grow_capacity"]
